Інструкція по рендеру круглої кнопки.

За замовчуванням type="button". Приймає єдиний пропс onClick.

Рендериться просто як <CircleButton/>,  без додаткових налаштувань чи {children}, вже з іконкою і відповідним текстом.

Mожна передавати пропси z, pos, l, r, t, b. В останні передавайте в лапках значення з пікселями, якщо передаєте ноль, то без пікселів
    <CircleButton z="999" pos="absolute" l="0" r="20px"/ >

